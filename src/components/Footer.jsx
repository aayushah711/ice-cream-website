const styles = {
    theme: { light: "#f8f9fa", dark: "#212529" },
    backgroundColor: { light: "#f8f9fa", dark: "#212529" },
};

const Footer = () => {
    return (
        <div style={{ backgroundColor: styles.backgroundColor.light }}>
            Footer
        </div>
    );
};

export default Footer;
